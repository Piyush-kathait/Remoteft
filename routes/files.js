const router = require('express').Router();
router.post('/', (req, res) => {
    //Validation of the request



    //store the files


    //stor in the database


    //response includes the file download link
});

module.exports = router;